# botloggi

Repositório Prado

A Python CLI application, managed with [uv](https://docs.astral.sh/uv/).

## Usage

```sh
uv sync
uv run botloggi
```

## Development

```sh
uv run pytest        # run tests
uv run ruff check .   # lint
uv run ruff format .  # format
```
